<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All events
    </h1>

    <a href="/events/new" class="btn btn-block btn-outline-secondary">
      Add a new event
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Name
        </th>

        <th>
          Discription
        </th>

        <th>
          Venue
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @events.each do |event| %>
      <tr>
        <!-- Display event.id start -->
        <td>
          <%= event.id %>
        </td>
        <!-- Display event.id end -->

        <!-- Display event.name start -->
        <td>
          <%= event.name %>
        </td>
        <!-- Display event.name end -->

        <!-- Display event.discription start -->
        <td>
          <%= event.discription %>
        </td>
        <!-- Display event.discription end -->

        <!-- Display event.venue start -->
        <td>
          <%= event.venue %>
        </td>
        <!-- Display event.venue end -->

        <!-- Display event.created_at start -->
        <td>
          <%= time_ago_in_words(event.created_at) %> ago
        </td>
        <!-- Display event.created_at end -->

        <!-- Display event.updated_at start -->
        <td>
          <%= time_ago_in_words(event.updated_at) %> ago
        </td>
        <!-- Display event.updated_at end -->

        <td>
          <a href="/events/<%= event.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
