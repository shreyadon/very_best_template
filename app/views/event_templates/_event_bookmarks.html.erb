<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Bookmarks

    <a data-toggle="collapse" href="#event_bookmarks_new_form">
      Add bookmark
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @event.bookmarks.each do |bookmark| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/bookmarks/<%= bookmark.id %>">
          <%= bookmark.notes %>
        </a>

        <%if current_user == bookmark.user %>
          <a href="/delete_bookmark_from_event/<%= bookmark.id %>">
            Delete
          </a>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="event_bookmarks_new_form">
        <%= render partial: "event_templates/event_bookmarks_new_form" %>
      </div>
    </div>

  </ul>
</div>
